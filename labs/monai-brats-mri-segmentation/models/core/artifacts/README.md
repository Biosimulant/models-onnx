Imported ONNX artifact exported from the published MONAI BRATS bundle's `model.pt`.

Validation status:
- `export-metadata.json` records the exported ONNX graph contract and validation
  output shape.
