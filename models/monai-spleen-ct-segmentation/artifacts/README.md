Imported ONNX artifact exported from the published MONAI spleen bundle's `model.ts`.

Validation status:
- `bundle-inspection.json` confirms the published NGC bundle contains `model.ts`
  and `model.pt`, not a prebuilt ONNX artifact.
- `export-metadata.json` records the exported ONNX graph contract and validation
  output shape.
