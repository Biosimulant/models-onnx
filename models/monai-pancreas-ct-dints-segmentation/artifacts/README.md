Imported ONNX artifact exported from the published MONAI pancreas DiNTS bundle's `model.pt`
and `search_code_18590.pt`.

Validation status:
- `export-metadata.json` records the exported ONNX graph contract and output shape.
